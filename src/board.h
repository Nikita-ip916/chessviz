#ifndef BOARD_H
#define BOARD_H
void Board(
        int board_size,
        int game_board[][board_size],
        int move_count,
        int move_size,
        int move_reader[][move_size],
        int i);
#endif
